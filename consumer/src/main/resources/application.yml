server:
  port: 8020
spring:
  application:
    name: consumer
eureka:
  client:
    service-url:
#                   注册中心访问地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true